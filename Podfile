# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MilluApplication' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MilluApplication
	pod 'Firebase/Core'
	pod 'Firebase/Database'

  target 'MilluApplicationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MilluApplicationUITests' do
    # Pods for testing
  end

end
